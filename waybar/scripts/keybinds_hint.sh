#!/bin/bash

rofi -e "🧠 Atajos de teclado:
      Super + Q → Abrir terminal
      Super + C → Cerrar ventana
      Super + M → Log Out
      Super + E → Explorador de archivos 
      Super + V → Act./Des. Flotante
      Super + R → Ejecutar programa
      Super + P → Act./Des. Responsive
      Super + J → Hrztl/vrtcl Responsive
      Super + D → Cambiar Wallpaper
      Super + ImpPnt → Recorte Pantalla
      Super + SHIFT + 1 → Mover ventana a espacio 1
      Super + Mouse Up / Down → Moverte a escritorio 1-2" \
    -config ~/.config/rofi/config-key-hint.rasi